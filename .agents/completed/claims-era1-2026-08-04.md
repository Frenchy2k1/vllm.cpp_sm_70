# Retired claims - era 1 (2026-07-02 .. 2026-08-04)

Claims whose every row left `SPIKE`/`ACTIVE` in the 2026-08-04 triage, kept
verbatim as the record of who held what. Nothing here is load-bearing for live
decisions: live ownership is in [coordination.md](../coordination.md) and the
current position is in [NOW.md](../NOW.md).

| Claim | Rows | Agent | Worktree / notes |
|---|---|---|---|
| `CLAIM-SWEEP-RECENT-DENSE` | `MODEL-TEXT-phi3-phi3-for-causal-lm`, `MODEL-TEXT-granite-granite-for-causal-lm`, `MODEL-TEXT-stablelm-stablelm-for-causal-lm`, `MODEL-TEXT-minicpm-mini-cpmfor-causal-lm`, `MODEL-TEXT-internlm2-intern-lm2-for-causal-lm`, `MODEL-TEXT-phi-phi-for-causal-lm`, `MODEL-TEXT-minicpm3-mini-cpm3-for-causal-lm` | Claude Code (opus-4-8) | isolated worktree `sweep-recent-dense` (scratch tree), no dgx dir | branch `sweep-recent-dense`, base `origin/main` `5c00fc4` | SPIKE ONLY, no implementation. Owns `.agents/specs/sweep-recent-dense-batch.md` plus the eight model-matrix rows above (batch triage: reuse-vs-new verdict, GB10 fit, tokenizer, oracle-risk, ranked queue + per-row W-orders). Each row is a separate future impl claim, one agent each. MUST NOT touch a model forward/kernel/loader or any golden. | `ACTIVE` | 2026-07-24 — batch-scoping spike, then TOP 3 IMPLEMENTED (batch3, worktree `sweep-recent-dense-batch3`, dgx `~/vllmcpp-b3`): **Granite-3 → `ACTIVE`** (SACRED 16/16 vs vLLM 0.25.0, K=5 deterministic → STRICT); Phi-3/Phi-4 implementation is HELD in a separate increment (gate not a clean pass; see its own commit); **OLMo-3 IMPLEMENTED but oracle-BLOCKED** (rides the OLMo-2 row under `CLAIM-SWEEP-OLMO2`; pinned vLLM 0.25.0 `KeyError: 'rope_theta'` on OLMo-3's per-layer-type rope schema → no SACRED bar). Regressions byte-identical (OLMo-2/Qwen3-dense/OPT/Llama/Mistral). **rank-4 StableLM → `ACTIVE`** (2026-07-26, worktree `sweep-stablelm`, dgx `~/vllmcpp-stablelm`): SACRED 16/16 vs vLLM 0.25.0 (per-prompt K=5 deterministic → STRICT; 14/16 exact + 2/16 near-tie, 0 divergent), ZERO new kernel (LayerNorm+partial-rope+qkv-bias reuse), one inert shared tokenizer canon (CR/LF→escape; pretokenizer 97926/97926). **rank-6 InternLM2 → `ACTIVE`** (2026-07-26, worktree `internlm2-bringup`, dgx `~/vllmcpp-internlm2`): SACRED 16/16 vs vLLM 0.25.0 (per-prompt K=5 deterministic → STRICT; 12/16 exact + 4/16 near-tie all gap 0.0 nats, 0 divergent), ZERO new kernel (reuses the Llama/Qwen3-dense forward VERBATIM; the ONLY delta a loader-side de-interleave of the fused `wqkv` interleaved-by-KV-group, `internlm2.py:158-176`; RED wrong-split CAUGHT at prompt0 tok0). Added an ADDITIVE engine `TokensPrompt` path (`LLMEngine::{add_request,generate}` / `InputProcessor::process_inputs_tokens`, string path byte-identical) to gate on the oracle's exact prompt ids — InternLM2's non-standard fast BPE (fuse_unk drops spaces) is not a supported tokenizer family; full port orthogonal/out of scope. **Command-R / Cohere (`CohereForCausalLM`) → `BLOCKED`** (2026-07-26, worktree `sweep-commandr`, base `origin/main` `0fb19410`, dev-box CPU build — dgx build disk-blocked): IMPLEMENTED faithfully (new files only, ZERO-NEW-KERNEL: weight-only Cohere LayerNorm + GPT-J full-width rope + PARALLEL residual + `logit_scale` + tied embeds, all grounded `commandr.py`); compiles+links+self-registers CPU `-Werror` clean; W0 oracle RUN-VERIFIED (`hf-internal-testing/tiny-random-CohereForCausalLM` builds+runs on vLLM 0.25.0, arch confirmed `CohereForCausalLM`≠`Cohere2`). SACRED gate BLOCKED: every real small `CohereForCausalLM` is HF-GATED (no dgx token), the only ungated vehicles are tiny-random (head_dim 8/2, outside validated attn), and dgx is disk-full (26 GiB free, no build tree) — moved OFF this claim (BLOCKED rows are unclaimed). **rank-8 Phi-1/Phi-2 (`PhiForCausalLM`, `microsoft/phi-2`) → `ACTIVE`** (2026-07-26, worktree `phi12-bringup`, dgx `~/vllmcpp-phi12`): SACRED 16/16 vs vLLM 0.25.0 (per-prompt K=5 deterministic → STRICT; 9/16 exact + 7/16 near-tie, max gap 0.25 nats, 0 divergent), the OLDER Phi arch DISTINCT from `Phi3ForCausalLM`. **ZERO-NEW-KERNEL** (the spike's predicted `kGelu` unary was unnecessary — `gelu_new` == the landed `vt::GeluTanh`): GPT-J parallel residual + nn.LayerNorm+bias + biased qkv/dense + partial NeoX rope + non-gated NewGELU MLP + untied biased lm_head, all reuse; F16→BF16 dtype-aware loader kept LOCAL to `phi_weights.cpp` (shared `dense_weight_loaders.h` untouched ⇒ new-files-only, every other model byte-identical). RED-first: dropped qkv bias 1.25 nats + sequential-residual 21.19 nats gate-CAUGHT, wrong rotary fraction hard-aborts; memcheck 0 errors. **rank-5 MiniCPM (`MiniCPMForCausalLM`, `openbmb/MiniCPM-2B-sft-bf16`) → `ACTIVE`** (2026-07-26, worktree `minicpm-bringup`, dgx `~/vllmcpp-minicpm`): SACRED 16/16 vs vLLM 0.25.0 (per-prompt K=5 deterministic → STRICT; 10/16 exact + 6/16 near-tie, max gap 0.0 nats, 0 divergent). ZERO new kernel = the landed Granite/Llama dense forward + three scalars (scale_emb 12 embedding scale; scale_depth/sqrt(40) scaled residual add per sublayer; hidden/scale_width=hidden_size/dim_model_base=9.0 before lm_head), tied lm_head. **W0 `.bin`-only risk resolved WITHOUT a C++ pickle loader**: converted the official openbmb `.bin`→safetensors via trusted torch (same weights, both oracle+engine read identical bf16); vLLM builds+runs it with `trust_remote_code=True` (neither transformers nor vLLM registers the `minicpm` config). RED dropping scale_depth → 256/256 divergent max 29.375 nats gate-CAUGHT. Gated via the additive `TokensPrompt` path feeding the oracle's exact prompt ids (MiniCPM's SP normalizer Prepend/Replace not ported — vehicle tokenizer.json re-expressed as Metaspace for construction; follow-up). One opt-in `--trust-remote-code` flag added to the two shared golden-capture scripts (default off = inert). **rank-9 MiniCPM3 (`MiniCPM3ForCausalLM`, `openbmb/MiniCPM3-4B`) → `ACTIVE`** (2026-07-26, worktree `sweep-minicpm3`, dgx `~/vllmcpp-minicpm3`, base `origin/main` `a8363c60`): SACRED 16/16 vs vLLM 0.25.0 (per-prompt K=5 deterministic → STRICT; 13/16 exact + 3/16 near-tie, max gap 0.0 nats, 0 divergent). The first MLA-attention MiniCPM and the row that **CLOSES the non-trivial recent-dense tier**. ZERO new compute kernel = the landed MiniCPM 3 scalars with attention swapped GQA→**MLA**, REUSING the landed DeepSeek-V2 MLA block; THREE faithful deltas — `is_neox_style=True` (via a NEW default-false shared `mla::MlaBlockDims::is_neox_style` field ⇒ DeepSeek/GLM byte-identical), LongRoPE-not-YaRN (phi3_long_rope short cache, mscale 1.0), q_lora-always. ONE reuse-not-new shared change: the FA-2 MLA prefill zero-pads qk_head_dim 96→128 to reuse the compiled hdim128 kernel (identity for DeepSeek d=192/GLM d=256). RED (MLA-specific): wrong rope style → first-token divergence gate-CAUGHT. **DeepSeek-V2-Lite re-gated 8/8** (shared-MLA non-regression). `.bin`-only vehicle resolved via trusted torch `.bin`→safetensors; gated via the additive `TokensPrompt` path. memcheck 0, CUDA `-Werror` clean. SPEED PENDING (EAGER). **TRIVIAL-TAIL DONE (2026-07-26, worktree `sweep-yi-internlm3`, dgx `~/vllmcpp-yi-il3`, base `origin/main` `4fff34f7`) — the recent-dense TEXT tier is CLOSED.** Both remaining rows are Llama aliases on the ACTIVE Llama row (`CLAIM-MODEL-LLAMA-3.2`), each W0 RUN-VERIFIED on the pinned oracle: **Yi** — VERDICT modern Yi IS the Llama arch (`architectures:["LlamaForCausalLM"]`), ZERO code delta, no `YiForCausalLM` alias (vLLM registers none); SACRED `test_yi_paged_engine` (`01-ai/Yi-Coder-1.5B-Chat`) 16/16 (13 strict + 3 near-tie, max gap 0.125 nats). **InternLM3** — VERDICT a PLAIN Llama alias (`registry.py:134`→llama, dynamic-NTK rope factor 6.0, NOT InternLM2+sliding-window); ONE additive registry-alias line in `llama_registry.cpp`; SACRED `test_internlm3_paged_engine` (`internlm3-8b-instruct`) 16/16 (14 strict + 2 near-tie, max gap 0.0 nats). Additive-only ⇒ `Llama-3.2-1B` re-gated 16/16 UNCHANGED. Gated ID-based via `TokensPrompt` (new `scripts/stage-tokenizer-metaspace.py`). All 8 recent-dense families now accounted for |
| `CLAIM-LOAD-SENTENCEPIECE` | `LOAD-SENTENCEPIECE` (engine-matrix); unblocks `MODEL-TEXT-mistral-mistral-for-causal-lm` (model-matrix, paged gate) | Claude Code (opus-4-8) | isolated worktree `/home/mudler/_git/vllm.cpp-sentencepiece` branch `feat/sentencepiece-tokenizer`, base `adca14c`; dgx `~/work/sp-src` CUDA `build-cuda`, all GPU work under one `flock $HOME/gpu.lock`, `git archive` transfer, big-model gates STANDALONE, foreground | worktree branch `feat/sentencepiece-tokenizer`, base `adca14c` | Owned files: `include/vllm/tokenizer/{bpe,tokenizer}.h`, `src/vllm/tokenizer/{bpe,tokenizer}.cpp`, `src/vllm/v1/engine/detokenizer.cpp` (SP dispatch only), NEW `tests/vllm/test_tokenizer_parity_mistral.cpp` + `tests/parity/goldens/tokenizer_mistral/{tokenizer.json,encodings.json}` + `tools/parity/dump_tokenizer_mistral.py` + NEW goldens `tests/parity/goldens/mistral_greedy_7b/`, `tests/CMakeLists.txt` (test row), plus `.agents/specs/sentencepiece.md`, engine-matrix, model-matrix (Mistral row), coordination, roadmap, ledger, state, README, BENCHMARKS. The ONLY tokenizer-touching agent. MUST NOT touch a model forward/kernel, safetensors/GGUF loader, or any non-Mistral golden — the byte-level tokenizer path stays byte-identical. | `ACTIVE` | 2026-07-23 — **LANDED (local commit `b9e5065`, not pushed).** SentencePiece encode/decode byte-exact vs vLLM 0.25.0 (HF tokenizers 0.22.2): `test_tokenizer_parity_mistral` 6/6·421, SACRED cross-check 0/45 mismatch; byte-level suites byte-identical (852+221+1175+2461); ASan+UBSan clean. **Mistral `test_mistral_paged_engine` now RUNS + PASSES 16/16** (15 strict + 1 near-tie gap 0.0000 nats, 0 divergent) — Mistral CORRECTNESS-DONE, SPEED pending. Regressions each STANDALONE: 27B 235/235, 35B 315/315, Coder 138, dense 184, OPT 63, Llama 92 UNCHANGED; DeepSeek-V2 hits a PRE-EXISTING base-`adca14c` MLA prefix-cache assertion (engine untouched; tokenization byte-identical 2461) — flagged to a KV/MLA owner. CUDA+CPU `-Werror` 0-warn; DSR 32; all three record checkers green. |
| `CLAIM-MODEL-OPT-125M` | `MODEL-TEXT-opt-optfor-causal-lm` | Claude Code (opus-4-8) | isolated worktree `/home/mudler/_git/vllm.cpp/.claude/worktrees/agent-abd2d21b50a1ca44e`; dev-box CPU build `build-cpu`; DGX build/gate `~/work/vllm.cpp-opt125m` (production flags, one `flock /tmp/gpu` per gate series) | `main` (worktree) | OPT (`OPTForCausalLM`) bring-up — the breadth-sweep CROSS-FAMILY additivity canary (rank 4, `.agents/specs/breadth-sweep-plan.md` §B.3). OWNS the new files: `include/vllm/model_executor/models/opt.h`, `src/vllm/model_executor/models/{opt_registry,opt_weights,opt}.cpp`, `src/vt/cuda/cuda_layernorm.cu`, `src/vt/cpu/cpu_layernorm.cpp`, `tests/vt/test_ops_layernorm.cpp`, `tests/vllm/models/{test_opt_load,test_opt_paged_engine}.cpp`, `scripts/opt-{materialize-checkpoint,oracle-capture}.py`, `tests/parity/goldens/opt_greedy/`, `.agents/specs/sweep-opt-125m.md`. APPEND-ONLY additions to shared files: `include/vt/ops.h` + `src/vt/ops.cpp` (the 3 NEW ops `vt::{LayerNorm,Relu,Add}` — new `OpId` values, typedefs, `LayerNormArgs`, validating wrappers; NO existing op touched), `include/vllm/model_executor/models/dense_weight_loaders.h` (+`LoadMergedBf16Vector` for merged bias vectors), `include/vllm/tokenizer/pretokenizer.h` + `src/vllm/tokenizer/pretokenizer.cpp` (+`SplitPattern::kGpt2` + its own scanner; the Qwen/Llama-3 scanner is UNTOUCHED), `include/vllm/tokenizer/tokenizer.h` + `src/vllm/tokenizer/tokenizer.cpp` (accept ByteLevel `use_regex=true`; +`EncodeWithSpecialTokens` + the `template_bos_`/`template_eos_` fields), `CMakeLists.txt` + `tests/CMakeLists.txt` (source/test rows), and the arch-list/count expectations in `tests/vllm/models/test_model_registry.cpp`, `tests/vllm/test_model_loader_gguf.cpp`, `tests/vllm/test_pretokenizer.cpp`. EXACTLY ONE shared BEHAVIOR line: `src/vllm/v1/engine/input_processor.cpp` `Encode` -> `EncodeWithSpecialTokens`, provably a no-op for every existing model (Qwen HF post_processor is ByteLevel and GGUF sets no template tokens ⇒ both template ids -1). MUST NOT touch the runner, scheduler, platforms, the attention registry, any CUDA kernel, `hf_config.{h,cpp}`, `dense_attn_block.h` (reuse only), or any `qwen3*` model file | `ACTIVE` | 2026-07-21 W0-W4 LANDED. vLLM 0.25.0 greedy MEASURED **DETERMINISTIC** on opt-125m (K=5, **0 multi-valued cells**) ⇒ the SACRED gate is **STRICT token-exact**, and we PASS it **6/6 prompts / 96/96 tokens** (`test_opt_paged_engine`, 36 assertions). Loader 958 assertions (all 197 tensors, no leftover); new-op unit test 5 cases/9230 assertions; GPT-2 pretokenizer goldens HF-oracle-generated (18 cases/97926 assertions). ADDITIVITY VERDICT: the MODEL seams (registry/config/loader/runner/KV) held 100% — 4 new model files, ZERO shared model-layer edit; the leaks were one layer down in the `vt::` op table (no LayerNorm/ReLU/Add) and the tokenizer (no GPT-2 split; a parsed-but-never-applied post-processor BOS that silently scored 0/6 until fixed). SPEED bar explicitly PENDING (head_dim 64 falls to the generic paged path; no decode CUDA graph) — row stays `ACTIVE`, not `DONE` |
| `CLAIM-MODEL-LLAMA-3.2` | `MODEL-TEXT-llama-llama-for-causal-lm` | Claude Code (opus-4-8) | isolated worktree `/home/mudler/_git/vllm.cpp-llama` (branch `llama-3.2-bringup`, base `af0b21b`); DGX build/gate `~/vllmcpp-accel` (rsync scratch copy, production flags: CUDA arch 121a + CUTLASS + Marlin + FA2 sm_121a + Triton AOT; whole GPU series under one `flock $HOME/gpu.lock`, big-model gates STANDALONE) | `llama-3.2-bringup` (worktree) | Llama-3.2 (`LlamaForCausalLM`) bring-up — the breadth-sweep CROSS-FAMILY DENSE additivity proof (Tier-2 rank 5, `.agents/specs/breadth-sweep-plan.md` §B.3). OWNS the new files: `include/vllm/model_executor/models/llama.h`, `src/vllm/model_executor/models/{llama_registry,llama_weights}.cpp`, `tests/vllm/models/{test_llama_forward,test_llama_load}.cpp`, `tests/parity/test_llama_paged_engine.cpp`, `tests/parity/goldens/llama_greedy_1b/`, `.agents/specs/sweep-llama-3.2.md`. GUARDED, byte-preserving generalizations to shared files: `include/vt/ops.h` (`RopeArgs` +4 default-0 llama3 fields), `src/vt/cuda/cuda_ops.cu` + `src/vt/cpu/cpu_ops.cpp` (a `Llama3ScaleFreq` helper in the RopeNeox + RopeCosSinCache kernels — no-op when scaling_factor<=0), `include/vllm/model_executor/models/dense_attn_block.h` (`MakeRopeArgs(cfg)` + qk-norm-optional AttnBlock via `has_qk_norm`), and ONE tokenizer parse fix `src/vllm/tokenizer/tokenizer.cpp` (`ExtractBosEos` handles a `Sequence` post_processor wrapping a `TemplateProcessing` — byte-preserving for Qwen/OPT/DeepSeek, all ByteLevel or top-level-TemplateProcessing). APPEND-ONLY additions: `CMakeLists.txt` + `tests/CMakeLists.txt` (source/test rows), the 6->7 arch-list/count in `tests/vllm/models/test_model_registry.cpp`, and one opt-in `--gpu-mem` flag in `scripts/qwen3-oracle-capture.py` (default preserves Qwen behavior). MUST NOT touch the runner, scheduler, platforms, the attention registry, `hf_config.{h,cpp}` (already llama3-aware), any CUDA compute kernel body beyond the guarded rope helper, or any `qwen3*`/`opt*`/`deepseek*` model file | `ACTIVE` | 2026-07-23 W0-W4 LANDED. vLLM 0.25.0 greedy MEASURED **DETERMINISTIC** on Llama-3.2-1B (K=6, **0 multi-valued cells**) ⇒ the SACRED gate is **STRICT token-exact**, and we PASS it **16/16 prompts (12/16 strict + 4/16 near-tie band, max gap 0.0000 nats, 0 forward-divergent)** — at all 13 divergent positions vLLM's OWN teacher-forced argmax given OUR prefix IS our token (gap 0.0000). ONE correctness-fatal tokenizer bug found+fixed (Llama's `Sequence` post_processor wrapping `TemplateProcessing` ⇒ BOS 128000 never prepended, silently 1/16); the CUDA prefill-argmax diagnostic proved the FORWARD correct 4/4 given vLLM's exact tokens, isolating the fault to the tokenizer. Loader 792 assertions; forward doctest 510; llama3 rope formula verified 2e-7 rel vs vLLM. Checkpoint `unsloth/Llama-3.2-1B` (ungated mirror; 401 on gated meta-llama weights). ADDITIVITY VERDICT: the dense MODEL seams held 100% — 3 new Llama files reusing the shared dense forward, ZERO edit to runner/scheduler/platforms/attn-registry/hf_config/any qwen3-opt model; shared touches are guarded byte-preserving primitives + the one-layer-down tokenizer leak. REGRESSIONS UNCHANGED: 27B 235/235, 35B 315/315, Qwen3-Coder 6/6, Qwen3-dense 16/16, OPT 6/6, DeepSeek-V2 8/8; `-Werror` 0-warn; memcheck 0; DSR 67 held. SPEED bar explicitly PENDING (head_dim 64 -> generic paged path; the FA2-toggle decode path is the Llama-3.2-3B/head_dim-128 W-next) — row stays `ACTIVE`, not `DONE` |
| `CLAIM-MODEL-MISTRAL` | `MODEL-TEXT-mistral-mistral-for-causal-lm` | Claude Code (opus-4-8) | isolated worktree `/home/mudler/_git/vllm.cpp-sweep-mistral` (branch `sweep-mistral`, base `96c5cbb`); DGX build/gate `~/work/sweep-mistral` (rsync scratch copy, production flags: CUDA arch 121a + CUTLASS + Marlin + FA2 sm_121a + Triton AOT; whole GPU series under one `flock $HOME/gpu.lock`, big-model gates STANDALONE) | `sweep-mistral` (worktree) | Mistral (`MistralForCausalLM`) bring-up — the breadth-sweep FIFTH-FAMILY near-additive dense proof (Tier-2 rank 6, `.agents/specs/breadth-sweep-plan.md` §B.3). OWNS the new files: `include/vllm/model_executor/models/mistral.h`, `src/vllm/model_executor/models/{mistral_registry,mistral_weights}.cpp`, `tests/vllm/models/{test_mistral_forward,test_mistral_load}.cpp`, `tests/parity/test_mistral_paged_engine.cpp`, `.agents/specs/sweep-mistral.md`. APPEND-ONLY additions: `CMakeLists.txt` + `tests/CMakeLists.txt` (source/test rows), the 7->8 arch-list/count + one resolve case in `tests/vllm/models/test_model_registry.cpp` (unsupported-example arch advanced Mistral->Gemma3). MUST NOT touch the runner, scheduler, platforms, the attention registry, `hf_config.{h,cpp}`, the RoPE/attention kernels, `dense_attn_block.h`, the tokenizer, or any `qwen3*`/`opt*`/`llama*`/`deepseek*` model file — Mistral adds NO new primitive (plain rope theta 1e6 no-scaling + qk-norm-optional + untied lm_head + null sliding_window all pre-existed) | `ACTIVE` | 2026-07-23 W0-W3 LANDED; MODEL correctness oracle-validated tokenizer-free. vLLM's own `mistral.py` is "Mistral adaptation of the LLaMA architecture", so Mistral IS the shared Qwen3/Llama dense forward with NO new primitive. **MODEL forward gate: 30/30 greedy tokens match vLLM 0.25.0** (29/30 STRICT token-exact + 1/30 near-tie band [p3 "E equals m c" last token: our 4608 ∈ vLLM's OWN {1782,4608}], 0 forward-divergent; vLLM greedy MEASURED deterministic on 4/5 prompts K=3) — fed vLLM's EXACT prompt tokens through our CUDA prefill, isolating the FORWARD from the tokenizer. W2 loader validated on real 3-shard weights (1541 assertions: untied lm_head [4096,32768], qk-norm empty, no leftover); CPU registry 299 + forward doctest 510; CUDA `-Werror` 0-warn; DSR 32 held. **REAL FINDING (blocker):** our ByteLevel-BPE-only tokenizer cannot load Mistral's SentencePiece/Metaspace tokenizer.json (Metaspace pre_tokenizer + byte-fallback vocab) → `FromHfJson` throws, so the FULL paged-engine SACRED gate is BLOCKED (the pre-inventoried `LOAD-SENTENCEPIECE` row (SentencePiece tokenizer family)); the tokenizer-free forward gate is what validates the MODEL. ADDITIVITY: git diff = only new files + CMake rows + registry test (zero shared model/kernel/runner/tokenizer/config edit). REGRESSIONS confirmed UNCHANGED (registry 299, Qwen3-dense forward 1031, Llama paged-engine SACRED 16/16); the MoE/GDN gates (27B/35B/Qwen3-Coder/OPT/DeepSeek-V2) are unaffected BY CONSTRUCTION (no shared file touched). SPEED + full paged gate both explicitly PENDING — row stays `ACTIVE`, not `DONE` |
| `CLAIM-MODEL-QWEN3-CODER` | `MODEL-TEXT-qwen3-moe-qwen3-moe-for-causal-lm` | Claude Code (opus-4-8) | `include/vllm/model_executor/models/qwen3_moe.h`, `src/vllm/model_executor/models/qwen3_moe_registry.cpp` (NEW W0), `include/vllm/model_executor/models/dense_attn_block.h` (NEW W1 #1 extract), `include/vllm/model_executor/models/qwen3_5_moe_block.h` (NEW W1 #2 expose), `src/vllm/model_executor/models/qwen3.cpp` (W1 #1 uses extracted header, byte-identical), `src/vllm/model_executor/models/qwen3_5.cpp` (W1 #2 RunMoeBlock + #3 no-shared guard, byte-identical for 35B), `CMakeLists.txt` (TU add), `tests/vllm/models/test_model_registry.cpp` + `tests/vllm/test_model_loader_gguf.cpp` (arch count 3→4 + list msgs); W2-W5: `qwen3_moe_weights.cpp`, `qwen3_moe.cpp`, tests | worktree `agent-a776e08b26b120347` (W0+W1) | Breadth-sweep model #1: Qwen3-Coder-30B-A3B (`Qwen3MoeForCausalLM`, BF16, 48L, GQA 32/4 head_dim 128, 128 experts top-8, NO shared expert, untied lm_head, NO GDN) — spike [sweep-qwen3-coder-30b.md](../specs/sweep-qwen3-coder-30b.md). COMPOSES the DONE Qwen3-dense attention (`qwen3.cpp`) + the DONE 35B MoE `MoeBlock` (`qwen3_5.cpp`) for a full-attention MoE — ZERO runner change (the `ENG-RUNNER-MODELSHAPE` gen already covers it; MoeBlock takes no gdn_meta). FOUR model-layer seams: #1 extract file-static dense `AttnBlock`, #2 expose file-static bf16 `MoeBlock`, #3 no-shared-expert guard, #4 NEW bf16 safetensors expert loader (all current expert loading is NVFP4). SPEED gap = no fast BF16 grouped-MoE GEMM (only NVFP4-Marlin) → W5. **W0+W1 LANDED 2026-07-21:** W0 = registry stub (REGISTER + full-attn-only KV + config validation; load/forward throw W2/W3) resolves via the clean model-factory seam (zero shared-array edit); W1 = the three behaviour-preserving refactors #1 extract (dense_attn_block.h) / #2 expose (qwen3_5_moe_block.h RunMoeBlock) / #3 no-shared guard — Qwen3-dense byte-identical, 35B byte-identical (guard inert for shared>0, RunMoeBlock uncalled by 35B). Gates: dgx CUDA `-Werror` 0-warn; **Qwen3-dense 0.6B+4B near-tie 16/16 + 27B 235/235 + 35B 315/315 UNCHANGED**; registry resolves `Qwen3MoeForCausalLM`; memcheck 0; CPU ctest green. **W2+W3 LANDED 2026-07-21 (worktree `agent-ab8fda33e0739e320`):** W2 = BF16 loader `qwen3_moe_weights.cpp` (`LoadQwen3MoeForCausalLMWeights`: merged qkv/o + per-head q/k norm via `dense_weight_loaders.h`; NEW bf16 per-expert loader — router gate + 128 experts×gate/up/down transposed to Matmul-B, NO shared expert; UNTIED lm_head loaded separately). W3 = forward `qwen3_moe.cpp` (`Qwen3MoeModel::Forward/ForwardDevice` composing the reused dense `AttnBlock` + the exposed `RunMoeBlock` per layer, NO GDN/shared, untied lm_head `vt::Matmul`) wired into the factory (W2/W3 stubs replaced). New files `src/vllm/model_executor/models/qwen3_moe_weights.cpp`, `src/vllm/model_executor/models/qwen3_moe.cpp`, `tests/vllm/models/test_qwen3_moe_load.cpp`, `tests/vllm/models/test_qwen3_moe_forward.cpp` + `CMakeLists.txt`/`tests/CMakeLists.txt` TU glue + `qwen3_moe_registry.cpp` factory wiring. GATES: dgx CUDA `-Werror` **0-warn**; load gate all 18867 tensors mapped (131746 assertions); forward doctest 3/3 (real-ckpt prefill argmax=**12095 " Paris"** — correct); **27B 235/235 + 35B 315/315 UNCHANGED**; memcheck **0 err / 0 leak**. W4 near-tie token-exact vs vLLM 0.25.0 → W5 bf16-fast-MoE + every-axis speed remain. **W4 LANDED 2026-07-21 (worktree `agent-acd65100650905428`, base `673b2a8`) — CORRECTNESS-DONE:** SACRED paged-engine greedy gate `tests/vllm/models/test_qwen3coder_paged_engine.cpp` vs vLLM 0.25.0. vLLM's OWN greedy is DETERMINISTIC on Qwen3-Coder (K=5 per-prompt, `scripts/qwen3coder-oracle-capture.py`: 0 multi-valued cells) → the STRICT-where-well-posed near-tie-robust gate applies (dense methodology; teacher-forced via `scripts/qwen3coder-neartie-gap.py`). **6/6 prompts PASS** (138 assertions): STRICT token-exact 4/6 + near-tie-band 2/6, max teacher-forced gap 0.125 nats, 0 forward-divergent; the 2 near-tie flips ARE vLLM's own argmax on our prefix (prefill-vs-decode near-ties, not bugs). Oracle uses vLLM's TRITON MoE backend (FlashInfer-CUTLASS OOMs the 57 GiB model on 119 GiB unified). GATES: dgx CUDA `-Werror` 0-warn; 27B 235/235 + 35B 315/315 UNCHANGED; memcheck 0. Goldens committed under `tests/parity/goldens/qwen3coder_greedy/`. **W5 LANDED 2026-07-21 (worktree `agent-aedba14acac54513f`, base `a1611c7`) — SHIPPED, SPEED GATE MISSED:** NEW op `vt::MoeGroupedGemmBf16` (structurally reusing the NVFP4 grouped-MoE counting-sort/tile scheduling, fp4 decode → direct bf16 `[K,N]` read; naive / BM=16 decode-WMMA / BM=64 prefill-WMMA regimes) + `MoeBlockBf16Cuda` replacing the per-expert host-gather loop with ~3 on-device grouped launches, **DEFAULT ON** (`VT_MOE_BF16_FAST=0` rolls back) + a `MoeBf16FastLayoutOk` layout guard (so the `nk==true` 35B-MTP producer falls through to the reference loop) + a HOST-MIRROR RELEASE via the existing `ReleaseHost()`/`ShouldReleaseHostWeights` residency mechanism. CORRECTNESS RE-VERIFIED AND STRICTER: goldens re-measured by the ratified procedure after the grouped GEMM's f32 accumulation order re-resolved bf16 near-ties — **6/6**, STRICT **4/6 → 5/6**, max gap **0.125 → 0.0000 nats**. Same-binary A/B: TTFT 3.4×, TPOT 1.59×. BINDING GRID vs GRAPHED vLLM 0.25.0 (triton MoE, 1024/128, c1/c2/c4/c8): TTFT 0.41/0.15/0.18/0.24×, TPOT 0.80/1.02/0.75/0.55×, output tput 0.65/0.75/0.63/0.49× ⇒ **every-axis parity NOT met**. `nsys`-attributed: prefill WMMA GEMM = 56% of GPU time at ~1.7% of bf16 peak (untuned tile); decode naive kernel = 21.3% at 55% bandwidth (block-starved at N=768). Named remaining levers: w13 gate+up fusion, deterministic split-K, bf16 decode CUDA graph, prefill tile/pipeline rework. GATES: `-Werror` 0-warn; 6/6; 27B 235/235 + 35B 315/315 UNCHANGED; memcheck 0 on the MoE GEMM + engine path. NEW test `tests/vt/test_ops_moe_grouped_bf16.cpp` 4/4. NOT pushed. **W6 LANDED 2026-07-21 (worktree `agent-a06b02439128761e3`, base `bb5d842`) — MoE-GEMM tile/pipeline rework + deterministic split-K:** two DEFAULT-ON kernel levers in `src/vt/cuda/cuda_matmul_nvfp4.cu`. (1) NEW `MoeGroupedGemmBf16WmmaPipe` — the W5 tile's dominant defect was its UNCOALESCED `[K,N]` weight stage (consecutive lanes `n_cols` elements apart) on a GEMM that is WEIGHT-BANDWIDTH bound; rewritten with a k-major `[BK][BN]` shared tile read as `wmma::matrix_b` ROW_MAJOR (global read along contiguous N), 16-byte vectorized stages, a 3-deep `cp.async` MULTI-STAGE pipeline, BN 64->128 and +8-half padded shared rows. Shape ported from vLLM `fused_moe.py:294` `fused_moe_kernel` + `:1238` `get_default_config`; multistage structure from CUTLASS `mma_multistage.h`. Guarded by `Bf16PipeShapeOk`, W5 tile retained for ragged pitches; `VT_MOE_BF16_PIPE=0` rolls back. (2) NEW DETERMINISTIC split-K for small-P decode (`MoeGroupedGemmBf16NaiveSplitK` + `...SplitKReduce` + graph-safe `EnsureMoePartials`) — mirrors vLLM's `SPLIT_K` (`fused_moe.py:338`) but with a FIXED ascending-split partial reduction, explicitly NOT `atomicAdd` (nondeterministic order would break greedy reproducibility and the SACRED gate); `VT_MOE_SPLIT_K=0` rolls back. KERNEL A/B: prefill **4.50 -> 12.15 TFLOP/s (2.70x)** (N=2048: 4.49 -> 12.48, 2.78x), decode-WMMA 2.84x, split-K 2.63x — **~1.2x vLLM's Triton `fused_moe` ~10.0 TFLOP/s**, closing the W5 headline kernel deficit. CORRECTNESS UNCHANGED, ZERO TOKEN MOVEMENT (the tile preserves the per-output K-reduction ORDER exactly => BIT-IDENTICAL to W5): **6/6, STRICT 5/6, max gap 0.0000 nats**, goldens md5-verified unchanged. `test_ops_moe_grouped_bf16` extended **4 -> 7 cases / 19 assertions** (aligned-pitch pipelined prefill/decode/down shapes with ragged K/N tails exercising the `cp.async` zfill + a run-to-run BIT-REPRODUCIBILITY assertion on split-K); the new decode case caught a real pipeline bug (tail iterations skipped `__pipeline_commit()`, under-counting `__pipeline_wait_prior`). DENOMINATOR CORRECTION retroactive to W5: W5's c2/c4/c8 vLLM numbers came from ONE shared `vllm serve` replaying seed-0 prompts (vLLM `RandomDataset` = `(offset+index+arange) % vocab_size`, `datasets/datasets.py:557-566`) => **43.6-63.5% prefix-cache hits** and an INFLATED denominator; W6 uses a FRESH server per concurrency, verified **0.0% hit rate** (c1 always clean, 324.04 -> 321.88 ms). BINDING GRID vs GRAPHED vLLM 0.25.0 (>1 = we win): TTFT **1.03/0.82/1.05/1.19x**, TPOT **0.88/1.01/1.22/1.05x**, ITL **0.88/1.03/1.22/1.09x**, output tput **0.77/0.90/1.09/0.99x** — **12/16 cells at/above vLLM, c4 EVERY axis, c8 3-of-4**; every-axis-at-every-concurrency NOT met. `nsys`-attributed residual: c1 decode = the MISSING bf16 DECODE CUDA GRAPH (summed kernel time ~31 ms vs 36.22 ms TPOT => ~86% GPU-busy, ~5 ms/step host tax ~= the whole deficit; kernels at 70-77% of peak bandwidth); c2 TTFT = the NON-MoE prefill glue (the MoE tile fell 56.0% -> 16.6% of GPU time). GATES: `-Werror` **0-warn** clean full rebuild; 6/6 + 7/7 + NVFP4 9/9 + forward 3/3 on the FINAL binary; **27B 235/235 + 35B 315/315 UNCHANGED**; memcheck **0 memory errors**. NOT pushed. **W7 LANDED 2026-07-21 (worktree `agent-ad451b300109c8df0`, base `a63c497`) — the bf16 DECODE CUDA GRAPH (W6 lever (i)):** NEW `Qwen3MoeDecodeGraph` (`include/vllm/model_executor/models/qwen3_moe.h` + `src/vllm/model_executor/models/qwen3_moe.cpp`), dispatched from `qwen3_moe_registry.cpp` on `pure_decode && is_cuda()` — the third sibling of the SAME in-tree driver as `Qwen3_5DecodeGraph` (35B) and `Qwen3_5DenseDecodeGraph` (27B), both fp4-gated so a bf16 model never got one. Ported from `vllm/v1/worker/gpu_model_runner.py::GPUModelRunner` + `vllm/compilation/cuda_graph.py::CUDAGraphWrapper.__call__` @ `e24d1b24`; reuses the shared `DecodeGraphSizes`/`PadToCaptureSize`. Coder-specific: attention-only padded-input builder (no GDN gate, so EVERY captured size is usable) + no defensive hidden copy; `ForwardBody` split into `EmbedInto` (outside the graph) + capturable `ForwardLayers` (split verified behavior-preserving, `VT_QWEN3MOE_CUDAGRAPH=0` -> 6/6). **ONE REAL LATENT BUG FIXED in shared glue:** `BuildStepInputs` (`dense_attn_block.h`) uploaded the RoPE identity row-index from a STACK-LOCAL vector — under capture that bakes a dead stack address every replay re-reads, producing a WRONG token (`prompt[0] tok=2`, 3555 vs 576); now served from a process-persistent per-T table (contents byte-identical). Latent, not a regression (35B/27B use `BuildStepDevInputs`; Qwen3-dense had no graph). **MEASURED (nsys c1):** GPU-busy **~86% -> ~92%**, host tax **~5 -> ~2.7 ms/step**, median TPOT **36.22 -> 34.13 ms**; 60 `cudaGraphLaunch` vs 3,569 total `cudaLaunchKernel`. **CORRECTNESS: ZERO TOKEN MOVEMENT** — 6/6 (138 assertions), STRICT 5/6, max gap 0.0000 nats, goldens md5-verified unchanged. **BINDING GRID** vs GRAPHED vLLM 0.25.0, FRESH server per concurrency, **0.0% prefix-cache hit VERIFIED**, ours 2 reps cold-leg-discarded (>1 = we win): TTFT **1.02/0.91/1.05/1.17x**, TPOT **0.93/1.12/1.27/1.08x**, ITL **0.93/1.10/1.28/1.14x**, output tput **0.84/0.96/1.11/1.03x** — **11/16 at/above vLLM; c4 AND c8 now pass EVERY axis**. Residual = 5 cells all at c1/c2: c1 = the remaining ~2.7 ms/step HOST tax (vLLM's whole 31.75 ms step ~= our kernels alone at ~31.5 ms, so no kernel work needed); the CUDA-API explanation was profiled and **REFUTED** (`cudaStreamSynchronize` median 4.5 us), so the next lever is a **CPU-side** profile of engine per-step bookkeeping. c2 is prefill-bound (non-MoE prefill glue), untouched by a decode graph. GATES: `-Werror` 0-warn clean rebuild; 6/6 ON + 6/6 OFF + 7/7 + 9/9 + 3/3 + dense green; **27B 235/235 + 35B 315/315 UNCHANGED**; memcheck **0 errors** on the decode path, engine path AND the full Coder decode-graph gate. NOT pushed. | `ACTIVE` | 2026-07-21 (W0-W7 landed; W7 on worktree `agent-ad451b300109c8df0`; correctness DONE with zero token movement, SPEED 11/16 grid cells at/above graphed vLLM with c4 AND c8 passing every axis and the 5 residual c1/c2 cells attributed) |
| `CLAIM-SWEEP-GEMMA` | `MODEL-TEXT-gemma-gemma-for-causal-lm`, `MODEL-TEXT-gemma2-gemma2-for-causal-lm`, `MODEL-TEXT-gemma3-gemma3-for-causal-lm` (Gemma-4 dropped from the active row list — W6 honesty pass moved it to `BLOCKED`, a terminal state; its characterization stays in the notes) | Claude Code (opus-4-8) | isolated worktree `/home/mudler/_git/vllm.cpp/.claude/worktrees/agent-gemma-spike` (spike only — no build, no GPU work, no download) | worktree branch `spike-gemma-family`, base `d85fd04` | SPIKE ONLY, no implementation. Owns `.agents/specs/sweep-gemma.md` plus the four Gemma text rows above. Covers: the complete Gemma registry inventory + per-version characterization (Gemma 1/2/3/4); the reuse-vs-new factoring (gemma-RMSNorm, sandwich norms, SentencePiece, sliding window, QK-norm recipe, tied embeddings all REUSE; GeGLU, final logit soft-cap, qpas/embed-scale, dual-rope routing NEW); the per-version attn-soft-cap-vs-QK-norm delta; measured per-variant GB10 hardware fit; and the Gemma-4 primitive stack (PLE, YOCO, MoE router, k_eq_v) with its gate-BLOCKED verdict. **HEADLINE:** newest registered = **Gemma 4** (real, public, but MM-wrapped-only checkpoints + 0.25.0 oracle-support unverified + PLE/YOCO/MoE campaign) leads the characterization; the recent-first gate vehicle that FITS + is oracle-certain is **Gemma 3** (`Gemma3ForCausalLM` on `google/gemma-3-1b-it`). The two primitives the task flagged (sandwich norms, SentencePiece) are BOTH already ours, plus sliding-window and QK-norm — Gemma reduces MOSTLY to reuse; the one genuinely-new compute kernel is GeGLU (`gelu_pytorch_tanh`+mul). **NO SHARED-WORK COLLISION:** the GeGLU activation and the final-logit-soft-cap are new but self-contained; no other running claim owns them. | `ACTIVE` | 2026-07-24 spike committed; **W0-W2 LANDED** (base `a20a11c`, worktree `gemma3-w0w2`, dgx `~/work/gemma3-w0w2`). W0: fetched `gemma-3-1b-it`, 0.25.0 oracle constructs Gemma3, K=5 per-prompt greedy ALL-DETERMINISTIC → STRICT bar, BOS-verified (prompt_ids[0]==2). W1: `kGeluAndMul` (GeGLU tanh) + `kMulScalar` (bf16 embed-scale), CUDA+CPU, unit gate `test_ops_activation` 12/12. W2: `gemma3.{h,cpp}`/`gemma3_weights.cpp`/`gemma3_registry.cpp` — tokenizer-free SACRED MODEL gate STRICT token-exact **48/48** greedy vs vLLM 0.25.0, loader gate 340 tensors, registry 23/23, clean `-Werror` 0 warn. OWNS new files: `gemma3.h`, `src/.../gemma3{,_weights,_registry}.cpp`, `tests/vllm/models/test_gemma3_{load,forward}.cpp`, `scripts/gemma3-oracle-capture.py`. ADDITIVE shared touches: `include/vt/ops.h`+`src/vt/ops.cpp`+`src/vt/cuda/cuda_ops.cu`+`src/vt/cpu/cpu_ops.cpp` (kGeluAndMul/kMulScalar appended before `kCount`, no op renumbered), `tests/vt/test_ops_activation.cpp`, `CMakeLists.txt`+`tests/CMakeLists.txt`, `tests/vllm/models/test_model_registry.cpp` (10→11 arch; ALSO repaired the pre-existing stale `Glm4MoeLiteForCausalLM` enumeration). **W3-W6 LANDED** (base `eb9d129`, worktree `sweep-gemma-w3w6`, dgx `~/work/gemma-w3w6`): W3 = `vt::SoftCap` (final cap) + `PagedAttentionArgs.logits_soft_cap` attention cap threaded into native/CPU/FA-2 attention (default-inert, diff-inert by construction). W4 `Gemma2ForCausalLM` (gemma-2-2b-it) near-tie-band SACRED **48/48** (44 strict + 4 at 0.0-nat vLLM-own ties; soft-cap PROVEN applied by cap-on≠cap-off A/B + unit + CPU differs). W5 `GemmaForCausalLM` (gemma-2b) STRICT **48/48**. W6 Gemma-4 honesty (HW/DEP-BLOCKED, not registered). Unit `test_ops_activation` 17/17, registry 389/389 (13 archs), loaders 1427+815, honesty 6/6, clean `-Werror` 0 warn, compute-sanitizer 0. Regressions byte-identical (Gemma-3 48/48, Qwen3-dense 184/184, OPT 63/63, Llama 92/92, Mistral 92/92, DeepSeek-V2 asserts-on 223/223). ADDITIVE new files: `gemma2.h`, `gemma.h`, `src/.../gemma2{,_weights,_registry}.cpp`, `src/.../gemma{,_weights,_registry}.cpp`, `tests/vllm/models/test_gemma2_{load,forward}.cpp` + `test_gemma_{load,forward}.cpp` + `test_gemma4_honesty.cpp`, `scripts/gemma-oracle-capture.py`. Shared touches: `include/vt/ops.h`+`src/vt/ops.cpp`+`src/vt/cuda/cuda_ops.cu`+`src/vt/cpu/cpu_ops.cpp` (`kSoftCap` appended before `kCount`), `src/vt/cuda/cuda_paged_attn.cu`+`src/vt/cpu/cpu_paged_attn.cpp`+`src/vt/cuda/cuda_flash_attn_fa2.cu` (softcap guarded, cap=0 byte-identical), `tests/vt/test_ops_activation.cpp`, `tests/vllm/models/test_model_registry.cpp`, `CMakeLists.txt`+`tests/CMakeLists.txt`. W7 (speed) NOT started. Rows Gemma-1/2 `ACTIVE` (correctness complete, speed PENDING — never `DONE` without vLLM every-axis parity); Gemma-4 `BLOCKED` |
| `CLAIM-SWEEP-OLMO2` | `MODEL-TEXT-olmo2-olmo2-for-causal-lm` (`Olmo2ForCausalLM` + `Olmo3ForCausalLM` — one vLLM class, one row) | Claude Code (opus-4-8) | isolated worktree `/home/mudler/_git/vllm.cpp/.claude/worktrees/agent-olmo2-spike` (spike only — no build, no GPU work, no download) | worktree branch `spike-olmo2`, base `a60b88b` | SPIKE ONLY, no implementation. Owns `.agents/specs/sweep-olmo2.md` plus the one OLMo-2 text row above. Covers: the complete OLMo registry inventory + per-arch characterization (OLMo-1/2/3, OLMoE, FlexOlmo, OlmoHybrid); the reuse-vs-new factoring; the norm-placement analysis; the QK-norm shape analysis; measured per-variant GB10 hardware fit; the SACRED gate design (form BY MEASUREMENT) + W-order; the upstream test inventory. **HEADLINE:** OLMo-2 is the CLEANEST dense bring-up yet — **ZERO new compute kernels**. The two things that look distinctive both reduce to WIRING over landed ops: (1) the **pure post-norm (`norm_after`) placement** is a strict SUBSET of the GLM-4/Gemma sandwich — it keeps ONLY the standalone-output-norm op (`glm4.cpp:174-178`, the exact primitive the task flagged) and DROPS the pre-norms, re-joining the residual with a plain add; (2) the **QK-norm is FULL-WIDTH, not per-head**, so it reuses `vt::RmsNorm` at a new `[T,q_size]`/`[T,kv_size]` shape but CANNOT use the fused per-head `kAttnQkNormRopeGate`. Everything else (plain RMSNorm, SiLU SwiGLU, NeoX rope, GQA paged glue, sliding-window (Olmo-3, via Gemma-3), tied embeddings, packed loader, ByteLevel BPE) is REUSE. Gate vehicle `allenai/OLMo-2-0425-1B` (1.485B, ~2.77 GiB, fits GB10 tight ~30 GiB free); Olmo-3 rides the same row (config-gated sliding window, 0.25.0 oracle-support UNVERIFIED — W0/W5 probe). OLMo-1 (non-parametric LayerNorm), OLMoE/FlexOlmo (MoE), OlmoHybrid (SSM) stay `INVENTORIED` with reasons. **NO SHARED-WORK COLLISION:** no new op, no shared-kernel edit — new-files-only plus ONE guarded additive tokenizer branch. | `ACTIVE` | 2026-07-24 W0-W4 LANDED (impl on base `origin/main` f312fd5). SACRED gate PASSES 16/16 on `allenai/OLMo-2-0425-1B` vs vLLM 0.25.0 (STRICT 13/16 + near-tie 3/16, max gap 0.094 nats, 0 forward-divergent; vLLM K=5 ALL-DETERMINISTIC → STRICT bar). W0 facts: config hidden 2048/16L/16H MHA/head_dim 128/rope_theta 5e5/UNTIED lm_head/F32-on-disk (loader downcasts f32→bf16); NO-BOS tokenizer (ByteLevel, `add_bos` unset) verified vs oracle; **Olmo3Config CONSTRUCTS in the 0.25.0 oracle** (both Olmo2/Olmo3 arch registered). New files: `olmo2.{h,cpp}`+`olmo2_{weights,registry}.cpp`+`test_olmo2_paged_engine.cpp`. ONE shared-TU touch: `tokenizer.cpp` accepts Split `Removed/invert=true` (guarded, additive). Not pushed. NEXT (out of this claim): W5 Olmo-3 sliding-window e2e, W6 speed. |
| `CLAIM-OLMO3-W0-VERIFY` | `MODEL-TEXT-olmo2-olmo2-for-causal-lm` (OLMo-3 W5 sub-clause of the OLMo-2 row; row stays `ACTIVE`, not moved to `DONE`). W0 authoritative oracle verdict + records-only honesty pass; the W5 code already landed in batch3 (`4e9f1d2e`). Owns EXACTLY the record edits: `.agents/model-matrix.md` (OLMo row note), `.agents/specs/sweep-recent-dense-batch.md` (§0.3, §0.5), `.agents/state.md`, `.agents/parity-ledger.md`, this claim row, `README.md`, `docs/BENCHMARKS.md`. **NON-COLLISION:** NO `src/`/`include/`/`tests/`/`CMakeLists.txt` touched — every SACRED gate byte-identical BY CONSTRUCTION (empty code `git diff`). No new op, no build, no golden. | Claude Code (opus-4-8) | isolated worktree `/home/mudler/_git/vllm.cpp/.claude/worktrees/w0-olmo3-verify` (branch `w0-olmo3-verify`); dgx oracle probe only `~/venvs/vllm-oracle` under `flock $HOME/gpu.lock` (sole owner, local-ai-worker Exited) | branch `w0-olmo3-verify`, base `origin/main` `bbaa182b` | OLMo-3 rank-1 recent-dense bring-up — VERIFY-FIRST W0: authoritatively resolve the "constructs vs runs" oracle contradiction before any implementation. | `DONE` | 2026-07-26 — **VERDICT: oracle-BLOCKED, RUN-VERIFIED on dgx.** vLLM 0.25.0 (transformers 5.13.1) cannot build `Olmo3ForCausalLM`: `olmo2.py:143` `rope_parameters["rope_theta"]` → `KeyError` (nested per-layer-type rope schema vs 0.25.0's flat expectation; full traceback captured). Checkpoint `allenai/OLMo-3-1025-7B` CACHED+ungated (~13.6 GiB). Config CONSTRUCTS via AutoConfig but MODEL does NOT run — the "CONSTRUCTS ⇒ unblocked" claim conflated the two; contradiction RESOLVED. NO SACRED bar (D5); W5 code DONE, gate PENDING oracle advance. Corrected the residual misleading records. OLMo-2 16/16 unchanged (byte-identical). Seven checkers green bare RC. Not pushed; FULL SHA reported. PIVOT → StableLM (Granite-3 already SACRED). |
| `CLAIM-PHI3-GATE-RESOLVE` | `MODEL-TEXT-phi3-phi3-for-causal-lm` (row `ACTIVE`; checklist mark `🚧`→`✅`; the held 15/16 increment closes). Resolves the held Phi-3/Phi-4 gate to a CLEAN PASS via the ratified near-tie ROOT-divergence methodology — TEST + GOLDENS ONLY, NO forward change (impl already correct per the exhaustive RCA). Owns EXACTLY: `tests/parity/test_phi3_paged_engine.cpp` (per-position → ROOT-divergence gate + the phi-4-14B bigger-dense anchor case + EOS-aware harness), the NEW golden dirs `tests/parity/goldens/{phi4_mini_greedy,phi4_14b_greedy}/`, `scripts/glm4-neartie-gap.py` (EOS-truncation robustness), and the phi3 record edits (model-matrix row + checklist, roadmap batch3 line, ledger, state, README, BENCHMARKS, this row). **NON-COLLISION:** NO `src/`/`include/`/kernel/loader/registry/CMake edit ⇒ every other model forward byte-identical BY CONSTRUCTION (`git diff --stat` = the phi3 test + 2 golden dirs + the capture script only). The `glm4-neartie-gap.py` change is a pure EOS-truncation robustness fix that cannot alter any committed golden or other gate (they read their own committed npy). | Claude Code (opus-4-8) | isolated worktree `/home/mudler/_git/vllm.cpp-phi3-wt` (branch `phi3-neartie-gate`); dgx build+capture+gate `dgx.casa:~/scratch_phi3_gate` (CUDA cutlass 4.5.0 `-DCMAKE_CUDA_ARCHITECTURES=121a` + FA2 + Triton banner confirmed, all GPU under `flock $HOME/gpu.lock`, sole owner) | branch `phi3-neartie-gate`, base `origin/main` `93ee0246` | Resolve the held Phi-3/Phi-4 (`Phi3ForCausalLM`) 15/16 gate to a clean pass via the ratified near-tie methodology (root-divergence gate + bigger-dense STRICT anchor); RCA re-verify, RED-first, records. | `DONE` | 2026-07-26 — **RESOLVED: gate now a CLEAN PASS.** RCA re-verified on current main (both checkpoints vLLM K=5 ALL-DETERMINISTIC; every ROOT divergence ≤0.5; p12 tok6/tok8 = 1.0 are CASCADE downstream of an exact-tie root tok4 gap 0.0000). **Phi-4-mini root-divergence gate 16/16** (7 strict + 9 near-tie, 0 divergent, 108 assertions), mirroring the landed olmo2/qwen3-dense gates. **RATIFIED BIGGER-DENSE STRICT anchor `microsoft/phi-4` (14B) 16/16 with STRICT token-exact 14/16** + 2 exact-tie (gap 0.0000), 134 assertions — RUN-verified (downloaded ~29 GiB, vLLM+our engine both run it). **RED-first:** disabling the LongRoPE mscale → 11 ROOT-fails (0.75–3.0 nats), gate FATAL 5/16; restored → 16/16. Harness+gap-script EOS-aware (14B emits EOS). NO src/kernel change; every other model byte-identical. All seven record checkers green by bare RC. Not pushed; FULL SHA reported. |
