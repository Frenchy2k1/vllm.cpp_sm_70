# Upstream sync cycle — 2026-07-30

- **Pin (from):** `555967922` (vLLM 0.26.0.dev0, advanced 2026-07-26, see
  [specs/pin-advance.md](../specs/pin-advance.md)).
- **Target (to):** `origin/main` HEAD `e04a30a77` (2026-07-30 14:56 +0800).
- **Range:** `555967922..e04a30a77` = **198 commits** over ~4 days.
- **Enumeration source:** the local vLLM git checkout `/home/mudler/_git/vllm`
  (`git fetch origin main`; real history, `git log --oneline 555967922..origin/main`).
- **Ported surface mapped:** `src/vllm/**` ↔ `vllm/**` 1:1 (per-file headers),
  `include/vllm/**`, `tests/vllm/**`.

This cycle is BOUNDED: it ports the single top CPU-buildable T1 change (below)
and leaves the rest as the ranked queue. **The pin is NOT advanced** — only a
subset landed; the next cycle picks up the carry-over.

## Ported this cycle

- **vllm#49754** `[Frontend] expose stream_interval as req sampling param` — T1,
  CPU-buildable. Adds a per-request `stream_interval` override of the engine-level
  `--stream-interval`, clamped up (only raises). Mirrored into
  `SamplingParams` (`include/vllm/sampling_params.h`, `sampling_params.cpp`
  Verify()), the output-processor clamp (`v1/engine/output_processor.cpp`
  FromNewRequest), and both OpenAI request paths (`entrypoints/openai/protocol.{h,cpp}`
  parse + to_sampling_params). Test ported from
  `tests/v1/engine/test_output_processor.py` into
  `tests/vllm/v1/test_output_processor.cpp` (3 cases). See parity-ledger row.

## Classification of the T0/T1-relevant subset

Only commits touching paths we mirror are tiered; the ~150 ROCm/XPU/Power/
KV-connector/distributed/CI/Rust-frontend commits are **N-A** (platforms and
subsystems we do not target — see Rules). Full ranked queue is
[specs/upstream-sync-2026-07-30.md](../specs/upstream-sync-2026-07-30.md).

### T0 — touch a gate model / gated correctness-or-perf path
| vllm# | subject | our file(s) | portable+CPU? | disposition |
|---|---|---|---|---|
| 49750 | RMSNorm uncontiguous support (1.2–3.1x kernel) | `src/vt/**/rms_norm*` (CUDA) | port structure yes / verify GPU | INVENTORY — DERIVED+BUILD-VERIFIED candidate, runtime gate QUEUED (perf, GPU-only) |
| 48391 | Fix batch invariance in RMSNorm by pinning block size | `src/vt/**/rms_norm*` (CUDA) | structure yes / verify GPU | INVENTORY — determinism fix; pairs with 49750; GPU gate |

### T1 — a model/feature we have ported
| vllm# | subject | our file(s) | portable+CPU? | disposition |
|---|---|---|---|---|
| 49754 | expose stream_interval req param | `sampling_params.*`, `output_processor.cpp`, `openai/protocol.*` | **yes** | **PORTED (this cycle)** |
| 49030 | Fix video temporal padding estimates | `multimodal/qwen3vl_processor.cpp:279` | yes | **ALREADY-CORRECT** — our ceil formula `((n+tp-1)/tp)*tp` == the fixed `n+(-n%tp)`; affected upstream models (qwen2_vl/glm4_1v/keye/…) not ported. No action. |
| 48245 | Fix num_output_placeholders preemption underflow | `v1/core/sched/scheduler.cpp`, `async_scheduler.cpp`, `request.*` | yes but LARGE | **SKIPPED (incr 2)** — unported prerequisite machinery: the 1:1 fix rebuilds the async stale-output path around `num_in_flight_tokens`+`num_stale_output_tokens`/`drop_stale_output`+`reset_prefix_cache(reset_running)` force-preempt+`skipped_waiting`+PP concurrent-batch pipeline, NONE of which this tree carries (`async_tokens_to_discard` is never set; no PP window → the underflow cannot occur). Requeued: land the machinery first. |
| 50297 | Fix P/D preemption race condition | `v1/core/sched/scheduler.cpp` | partial (P/D is a KV-connector path) | **SKIPPED (incr 2)** — depends on 48245's `drop_stale_output` + KV-connector producer/consumer `requires_kv_delivery` role; no connector role in our preempt path, P/D transport N-A by policy. Requeued behind 48245 + KV-connector P/D. |
| 32e657e (49343) | eagle draft max position embeddings | `config/speculative.h` (not `v1/spec_decode/**` — the fix is config-level; queue mapping imprecise) | yes | **PARTIAL-DONE (incr 2)** — ported the pure clamp helper `MaybeOverrideDraftMaxPositionEmbeddings` + 3 unit tests (`test_speculative_draft_max_position_embeddings` 4/4 CPU, RED-first). No live call site yet (eagle draft loading deferred / EAGLE3 T2); wired in with the eagle loader. 2 model-integration tests SKIPPED (need HF ModelConfig); bundled `llm_base_proposer.py` block_size half N-A (no ported eagle proposer). |
| 49483 | compressed-tensors: prioritize fused name matches in find_matched_target | `model_executor/layers/quantization/compressed_tensors/**` | yes | INVENTORY — quant target-matching order; confirm our matcher ordering |
| 48589 | Enhance extra_config layer-name suffix matching | quant config | yes | INVENTORY — quant config resolution |
| 0da6e7f (49134) | Reject contradictory custom-op directives | `config/**` | yes | INVENTORY — config validation |
| ad5d29d (50210) | Support Qwen3.5 text-only dense+MoE | `model_executor/models/qwen3_5*` | yes (large) | INVENTORY — model add; we already carry qwen3_5 files; diff for behavior drift |
| dbccc5a (48912) | Enable EVS for Qwen3.5 | `qwen3_5*` | yes | INVENTORY — EVS video-token pruning feature; queue with 60915c9 |
| 60915c9 (47750) | Add VidCom2 video token pruning | multimodal | yes | INVENTORY — new MM feature, not ported |

### N-A (out of scope — recorded reason)
- ROCm / AITER / gfx / Quark, XPU / Intel, Power / s390x, SM107/Rubin, NIXL /
  MoRI-IO / KV-connector P/D transport, Elastic-EP, DeepEP, Rust-frontend/gRPC,
  CI/buildkite/docs, libtorch_stable, torch.compile/Inductor plumbing, Kimi-K3
  Python/Rust frontend + CuTe-DSL NVIDIA kernels (arch we don't gate). ~150 of
  198. These touch subsystems the project explicitly does not target
  (upstream-sync.md Rules / discipline.md deviations §9).

## Carry-over (next cycle)
The T0 RMSNorm perf/determinism pair (49750+48391) and the remaining T1 queue
(49483 quant matcher, then the eagle-loader wiring of the landed 49343 clamp) are
ready ports; ranks in the spec doc. **Incr 2 (2026-07-30)** ported the 49343
clamp helper (CPU 4/4) and SKIPPED 48245+50297 as unported-code-path (async
`num_in_flight_tokens`/stale-output + reset-running force-preempt + KV P/D
machinery absent) — requeued as scoped machinery-first ports. Pin stays
`555967922` until the T0 gate re-runs on GB10.
