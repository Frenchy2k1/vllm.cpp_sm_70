# Upstream sync cycle — 2026-08-21

- **Pin (from):** `555967922` (vLLM 0.26.0.dev0, advanced 2026-07-26, see
  [upstream-sync.md](../upstream-sync.md)).
- **Target (to):** `origin/main` HEAD `2adf4b9e2a` (2026-08-21).
- **Range:** `555967922..2adf4b9e2a` = **1111 commits** over 26 days.
- **Note:** the prior reference HEAD `bf2866f8bf` (2026-08-20) sits 1050 commits
  past the pin; its delta was never recorded as a sync cycle, so this report
  covers the full window, not just the last day.
- **Enumeration source:** local vLLM checkout `/home/nvidia/Dev/references/vllm`
  (fetched 2026-08-21, fast-forwarded to `2adf4b9e2a`). The ported surface is
  derived from per-file headers (389 upstream paths named in our tree)
  intersected with the changed `vllm/` files: **50 files** in **393 unique
  commits** touch the mirrored surface. The remaining 718 window commits touch
  nothing we mirror.
- **Other references (fetched, no new parity surface):** `v100-skinny` 0 new
  (local feature branch `1cat-turbo`, 3 behind its `origin/main`);
  `flash-attention-v100` 0; `BetterBench` 0; `exllamav3` 26 ahead on
  `origin/dev` (not a vLLM oracle); `1Cat-vLLM` 2 new branches
  (`v100-dflash2-sm70-*`). Only the registered oracles are port sources
  (AGENTS.md §oracles), so `1Cat-vLLM` is feature work in a fork, not a mirror
  source. vLLM is the only reference in this cycle.

**Scope:** triage and report only. **No code ported this cycle; the pin is NOT
advanced** (protocol step 7: a stalled cycle keeps the old pin and records
carry-over). The one PORT-NOW item is queued for the next session.

## Triage summary

| Disposition | Count | Notes |
|---|---|---|
| **PORT-NOW** | 1 | 47272, a one-line capacity fix |
| **ALREADY-CORRECT** | 7 | the tree is already at, or ahead of, the upstream behavior |
| **INVENTORY** | 14 | T1/T2 candidates and one oracle-baseline flag, below |
| **N-A / IGNORE** | 371 | subsystems not carried, by group below |

The 393 are dominated by Model Runner V2 (55), DeepSeek-V4 (20), Kimi-K3 (10),
and MLA (20) work, none of which this tree carries.

## PORT-NOW (queued, next session)

| vllm# | change | our status | action |
|---|---|---|---|
| 47272 | `kv_cache_utils.py:892,2228` — `check_enough_kv_cache_memory` and `get_kv_cache_configs` now plan against `available - _pool_bytes_per_block`, so the null block that `BlockPool` permanently holds back is reserved at capacity-check and auto-fit time | Our `BlockPool` pops the null block identically (`block_pool.cpp:53-57`) and `allocate_slots` plans from the same pool. Not yet verified whether the C++ startup capacity planner (the analogue of `check_enough_kv_cache_memory`) starts from the pre- or post-null-subtraction count | Mirror the diff (2 sites, ~6 lines) plus a red-first capacity test; confirm the planned block count shifts by exactly the null-block bytes |

## ALREADY-CORRECT (no action; recorded so the next cycle does not re-flag)

| vllm# | upstream change | why the tree is already correct |
|---|---|---|
| 53077 | `gdn_attn.py:239` — reset `num_spec_decodes = 0` in the empty-draft branch | Our build derives `num_spec_decodes` inside the `draft_sum != 0` branch (`gdn_attn.cpp:126-142`); the else-path leaves 0. Same invariant, unconditionally. |
| 52078 | `gdn_attn.py:228-356` — spec/non-spec mask refinement, caching `non_spec_sequence_masks_cpu` | Behavior-neutral here: `num_accepted_tokens` is always null, so a spec row counts as a non-spec row under both the old and the new formulation, and the diff is dead code in this tree. If spec-decode acceptance lands with the MoE phase, re-run this classification against the then-current `gdn_attn.cpp`. |
| 53106 | `model_loader/utils.py` and the qwen models — fold `skip_prefixes=["mtp."]` into `hf_to_vllm_mapper` | No behavior change: the loader never requests `mtp.*` tensors, and the MTP draft head loads only when speculative decoding is enabled (`qwen3_5_weights.cpp:1411-1413`). |
| 50355 | `qwen3_5.py:303` — `hf_to_vllm_mapper` strips the `model.language_model.` prefix for text-only checkpoints | The dual-namespace backbone voting already accepts both spellings and refuses a mixed one (`qwen3_5_weights.cpp:1178-1194`). |
| 50704 | `qwen3_5.py:108` — accept `Qwen3_5MoeTextConfig` for transformers 5.x | The loader resolves every tower dtype and expert layout by tensor presence, not by HF config class name, so the transformers version is irrelevant to loading. |
| 51866 | `topk_topp_sampler.py:200` — seed-loss guard `len(generators) != n` changed to `not generators` | The bug presumes per-row generators, which this tree does not carry: the sampler uses one batch-wide `kDefaultSeed` with the row index in the hash (`sampler.cpp:26-31,347-358`). The mixed-seed path is structurally absent. |
| 51875 | `kv_cache_utils.py:87-114` — make the `NONE_HASH` default seed deterministic across processes (`"vllm-none-hash"`) | The tree is already deterministic by default: `init_none_hash` falls back to `kDefaultNoneHashSeed = "vllm.cpp/none_hash/v1"` (`kv_cache_utils.h:226`), and `EnsureNoneHash` runs at engine load (`model_loader.cpp:764-777`). |

**Recorded deviation (seed literal).** Upstream now uses `"vllm-none-hash"`;
this tree keeps `kDefaultNoneHashSeed` because that value is part of the
on-disk cache identity (`kv_cache_utils.h:225`). The only observable difference
is the block-hash bytes for a cross-process cache shared with a Python-vLLM
peer; sharing among our own processes is unaffected.

## INVENTORY (not scheduled; tier and one-line shape)

| vllm# | item | tier and shape |
|---|---|---|
| 50809 | `EngineCoreReadyResponse.mamba_block_size` — sync the scheduler's mamba block size to the frontend | T2. New engine-to-frontend field; lands with the spec and GDN engine work that produces the align-mode block size. |
| 4988df2 | `_max_num_batched_tokens` default gains a tier of 16384/1024 for 160 GB and above; the 70 GB tier keeps 16384 | T0 flag, no code change for 16 GB hosts (still 8192). Oracle-baseline flag: a future pin advance must re-measure the vLLM denominator, because upstream's default batching tier moved even though ours did not. |
| 52216 | `--prefix-cache-retention-interval` promoted to a CLI arg, default 0 | T2. New `CacheConfig` field plus arg. |
| 46175 | Completion API accepts `logprobs=-1` | T2. One validator line in the completion protocol. |
| 49519 | Defer post-load attention weight processing (`model_loader/utils.py:97`) | T2. The loaders are eager, so this is behavior-neutral today; queue it if a loader restructure lands. |
| 51446 | Preserve revision pins in secondary artifact loaders | T2. Affects revision resolution for side artifacts such as `weights_model`. |
| 51665 | Weight tying: `maybe_untie_word_embeddings` — when the config claims `tie_word_embeddings=true` but the checkpoint ships its own `lm_head`, upstream now loads it, compares, and re-ties if equal | T1 candidate. The loaders trust the config flag only (`qwen3_5_weights.cpp` dense arm: `w.lm_head` loads only when `!tie`). Gated NVFP4 checkpoints are tied, so no behavior change there; the gap is a text-only Qwen3.5 checkpoint whose config claims tied but ships an untied `lm_head`, which the current loader would silently drop. Port = a checkpoint-metadata probe for `lm_head.weight` in the index, plus compare-and-re-tie after load. |
| 52147 | Standardise tying on `ParallelLMHead.tie_weights` (qwen3/qwen3_5) | T1 refactor that pairs with 51665. Python-side only; adopt together when 51665 lands. |
| 51695 | Move fused shared-expert selection to `fused_moe/utils.py` (from ROCm aiter ops) | T2. The MoE path does not carry the aiter/FSE feature, so there is no behavioral arm to mirror today. |
| 50210 | Qwen3.5 text-only dense and MoE support: register `Qwen3_5TextConfig`/`Qwen3_5MoeTextConfig`, add `IsHybrid` and the `get_mamba_state_*` calculators | Already-correct for the loader (the dual-namespace arms and the MTP opt-in are covered above). The `MambaStateShapeCalculator`/`CopyFunc` machinery is absent from the C++ tree; the GDN state shape is computed directly in the KV-spec path, so nothing is owed. Recorded so the next cycle does not re-flag the model file. |
| 50273 | Honor `--linear-backend` for ModelOpt W4A16 | T2 kernel dispatch. |
| 51738 | Avoid GPU-to-CPU syncs on the model-execution path (`v1/attention/backend.py`) | T2 perf. |
| 49665 | Standardize request error handling on the `VLLMError` hierarchy | T2 refactor; error-surface conformance. |
| 50297 | Fix the P/D preemption race | T2. Requires the KV-connector P/D role, which is not carried. |

## N-A (subsystem-level; recorded, not per-commit)

Counts are over the 393 mirrored-surface commits, full window; the rest of the
1111 window touches nothing we mirror.

| Subsystem | ~# | reason |
|---|---|---|
| Model Runner V2 (`v1/worker/gpu/model_runner.py`) | 55 | The tree carries an independent `runner.cpp` (block-table/prepare-ports lineage), not an MRV2 mirror. Encoder graphs, routing, MRV2 spec/MTP, weight offloading, E/P/D disaggregation, and the attention-free model path are all MRV2-only. |
| DeepSeek-V4 NVIDIA kernels and DSV4 perf/MTP/SP | 20 | DSV4 CUDA-kernel work (MegaMoE, mHC broadcast, sparse-MLA kernels, eager-graph narrowing) targets the compiled DSV4 path that is not carried. |
| MLA / sparse-MLA / Kimi-K3 / DSpark / DFlash | 35 | MLA chunked-context scheduling, Kimi-K3 DCP/DCSpark, DSpark confidence-scheduled verification, and the FA4-SM100 headdim-256 kernels are not mirrored. |
| ROCm / AITER / Quark / XPU / CPU-AMX / gfx1250 | 20 | Platforms that are not targets. |
| Linear-kernel backend registry (`kernels/linear/__init__.py`) | 15 | B12X/MXFP8/TensorRT-LLM backend selection; GEMM dispatch here is a fixed arm ladder, not a registry. |
| Multimodal (EVS, VidCom2, EPD, processor cache) | 15 | The MM subsystem; the Qwen3.5 port is text-only. |
| Scheduler non-core (KV-connector offload, P/D, PP window, LB, encoder cache) | 15 | None of the connector, PP, or async-receive machinery is carried. |
| Frontend serving (session ids, priority-from-header, routed-experts offset, count_reasoning_tokens, spec acceptance stats, Cohere v2, responses-render tests, non-object-JSON guards) | 25 | New OpenAI-API surface, not tiered yet. Individually small; group-queue when a T2 serving row opens. |
| Structured output (xgrammar stop-token masking, MistralCommonBackend tokenizers, FSM-after-reasoning fix, validator errors, NUL-byte reject) | 8 | There is no structured-output path. |
| Config and engine plumbing (Blackwell CUDA-graph-capture default 1024, bitsandbytes to OOT, `override_attention_dtype` removal, encoder-CG flag, NVFP4-KV scale search, NVML re-init guard, JIT-warmup infra, data-parallel port and rank guards, log noise) | 15 | Defaults and platform guards for hardware or subsystems that do not run. |
| Model adds not ported (Muse Glimmer, Kimi-K3 frontend, Dots3 NOTE MM, laguna/granite/internlm2/gemma/gpt2/minicpm/glm4/dsv4 model-file polish) | 10 | New models; port only if tiered. |
| Python-only refactors, dead-code removal, mypy | 15 | `AutoWeightsLoader` kwarg reduction, dead-code sweeps, type annotations. No behavior delta in the mirrored surface. |

## Carry-over (next session)

1. **PORT 47272** (null-block capacity reservation). The only PORT-NOW item;
   small, CPU-testable, red-first.
2. When a T1 serving or loader row opens, batch the INVENTORY items: 51665 and
   52147 (the weight-tying pair), and 46175, 52216, 51446 (small protocol and
   config args).
3. **50809** (mamba_block_size sync) rides with the GDN and spec-decode engine
   work, the MoE phase, not as a standalone port.
4. **The 51875 deviation is recorded.** Keep `kDefaultNoneHashSeed`; if the
   next cycle touches `kv_cache_utils`, document the literal difference in the
   porting-inventory §9 deviations.
5. **51866, 53077, 52078, 53106, 50355, 50704 are ALREADY-CORRECT.** Re-flag
   only if the underlying surface changes: per-row generator support,
   spec-decode acceptance, or MTP loading from the main loader.
6. **The pin stays `555967922`** until the PORT-NOW queue lands and the gate
   re-runs. The next cycle's range is then this report's target to the new HEAD.